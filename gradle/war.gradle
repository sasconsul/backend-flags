apply plugin: "war"

bootWar {
    mainClassName = "net.sasconsul.s2f.BackendFlagsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
